# Русская локализация интерфейса

Дата: 2026-08-22
Базовый коммит: `28c51ba`
Статус: черновик для ревью

## Цель

Добавить в интерфейс вторую локаль (русскую) и переключатель языка, сохранив
английскую версию как эталон. Терминология правил Kill Team при переводе не
меняется, и это проверяется автоматически, а не на глаз.

## Объём

**Входит:** все пользовательские строки клиента — `public/app.js` (7892 строки,
27 render-функций), словари, рантайм `t()`, переключатель языка в UI, глоссарий
защищённых терминов и автотесты.

**Не входит (осознанно):**

- Сообщения об ошибках с сервера. `ValidationError` и `HttpError` из
  `src/domain/validation.js` и `src/api/*` (~235 строк) приходят на клиент
  готовым текстом и показываются через `setMessage(err.message)`. Пользователь
  с русским интерфейсом увидит английскую подсказку при ошибке валидации. Это
  известный компромисс и очевидная точка следующей итерации: серверу
  понадобится отдавать код ошибки вместо текста либо учитывать
  `Accept-Language`.
- SEO-мета, canonical, `robots.txt` и `sitemap.xml` из `src/http/seo.js`.
  Остаются английскими, `hreflang` не добавляется.
- Пользовательский контент (имена игроков, описания турниров в markdown) —
  переводу не подлежит по определению.

## Подтверждённые решения

1. **Язык по умолчанию.** При отсутствии сохранённого выбора — по языку
   браузера: `navigator.language`/`navigator.languages` начинается на `ru` →
   русский, иначе английский. Явный выбор в `localStorage` всегда важнее.
2. **Механизм.** Ключи-идентификаторы (`t("tournaments.standings.title")`), а не
   английский текст в роли ключа. `en.js` остаётся полноценным словарём, а не
   только списком ключей, поэтому глоссарные проверки сравнивают `en[key]` с
   `ru[key]` и не теряют силу.
3. **Граница перевода.** Термины правил Games Workshop и названия отрядов
   остаются английскими. Турнирный слой переводится русскими устоявшимися
   эквивалентами (раунд, таблица, швейцарка, Бухгольц, бай, винрейт).
4. **Проверка терминов.** Глоссарий-файл как источник правды плюс автотесты в
   `test/unit/i18n.test.js`. Ручная вычитка — дополнение к тестам, не замена.
5. **`Strength of Schedule` и `SoS`** остаются английскими.
6. **`Classified` / `Non-Classified`** остаются английскими: это классификация
   отрядов в All Kill Team Challenge, имя собственное фичи.
7. **`Faction`** переводится как «Фракция» — но только подпись поля, не
   значение: в значении лежит название отряда, а оно защищено.

## Архитектура

### Файлы

```
public/game-data.js      игровые справочники (Tac Ops, Crit Ops, Killzones и др.)
public/i18n/en.js        эталонный словарь
public/i18n/ru.js        русский словарь
public/i18n/glossary.js  PROTECTED / FORBIDDEN / SAME_IN_BOTH
public/i18n.js           рантайм: детект локали, t(), plural(), формат дат
docs/i18n-glossary.md    политика перевода и обоснования
test/unit/i18n.test.js   семь проверок словарей и глоссария
```

Сборщика в проекте нет, `public/app.js` — классический скрипт без `import`/
`export`. Файлы подключаются обычными тегами; `defer` гарантирует порядок
исполнения:

```html
<script src="/game-data.js?v=..." defer></script>
<script src="/i18n/en.js?v=..." defer></script>
<script src="/i18n/ru.js?v=..." defer></script>
<script src="/i18n.js?v=..." defer></script>
<script src="/app.js?v=..." defer></script>
```

`glossary.js` в `index.html` не подключается: он нужен только тестам, браузер
его не загружает.

Версионный суффикс `?v=` ставится по существующему в `index.html` соглашению и
обновляется вместе с остальными при выкладке.

### Двойное потребление файлов

Справочники, словари и глоссарий читаются и браузером, и Node (тесты). Без
модульной системы это решается двумя строками в конце каждого файла:

```js
const TGTV_I18N_RU = { /* ... */ };
if (typeof module !== "undefined") module.exports = TGTV_I18N_RU;
```

В браузере `const` верхнего уровня классического скрипта попадает в глобальную
лексическую область и виден следующим скриптам. В Node срабатывает
`module.exports`, и тест делает `require("../../public/i18n/ru.js")`.

### Вынос игровых справочников

Из `public/app.js` в `public/game-data.js` переезжают шесть таблиц верхнего
уровня: `tacOpOptions` (`app.js:190`), `critOpOptions` (`app.js:205`),
`killzoneOptions` (`app.js:217`), `gameSystemOptions` (`app.js:84`), `seasons`
(`app.js:228`) и `venueModeOptions` (`app.js:237`).

Причина — не эстетика, а устранение дублирования: значения этих таблиц входят в
PROTECTED, и `glossary.js` должен их знать. Без выноса пришлось бы скопировать
29 строк вручную, и копия разошлась бы с оригиналом при первом же новом
даташлейте. После выноса `glossary.js` подтягивает их через `require`, а
названия отрядов берёт из уже существующего `KILL_TEAMS` в
`src/domain/kill-teams.js`.

Переезд механический: содержимое таблиц не меняется, `app.js` продолжает
обращаться к ним по тем же именам. Единственное исключение — `venueModeOptions`,
где поле `label` содержит текст интерфейса, а не данные: оно превращается в
`labelKey`, а сами подписи уезжают в словари (`Tabletop Simulator` попадает в
`SAME_IN_BOTH`, `In Real Life` переводится). Это делается сразу при переезде,
чтобы таблица не правилась дважды.

### Структура ключей

Плоская карта с точечными именами. Плоская, а не вложенная: сравнение множеств
ключей в тестах становится однострочным, при этом группировка по экранам
сохраняется порядком строк в файле и комментариями-разделителями.

Пространства имён по экранам: `common`, `nav`, `auth`, `play`, `games`,
`tournaments`, `stats`, `challenge`, `profile`, `feedback`, `admin`.

### Рантайм

```js
function t(key, vars) {
  let value = dict(locale)[key];
  if (value === undefined) value = dict("en")[key];
  if (value === undefined) { warnMissing(key); value = key; }
  return interpolate(value, vars);
}
```

Фолбэк-цепочка `ru[key] → en[key] → key` закрывает главный риск выбранного
подхода: забытый русский ключ даёт корректный английский текст, а не голый
идентификатор на экране. `warnMissing` пишет в `console.warn` вне продакшена.

**Интерполяция** — плейсхолдеры `{name}`: `t("games.count", { n: 5 })`.

**Плюрализация** — `plural(key, n, vars)`. Значение в словаре — объект с
формами, выбор через `Intl.PluralRules(locale)`: для английского `{ one, other }`,
для русского `{ one, few, many, other }`. Нужна как минимум для `${row.games}
games` (`app.js:3425`, `app.js:3455`), `${detail.wins} wins` (`app.js:3455`),
`${size} players` (`app.js:5881`, `app.js:6086`) и `player.games === 1 ? "game"
: "games"` (`app.js:3098`, `app.js:3119`).

**Экранирование.** `t()` не экранирует результат: строки словаря статические и
написаны нами, а подставляемые значения экранируются вызывающим кодом через
существующий `escapeHtml`, как и сейчас. Дисциплину закрепляет тест 6.

**Даты.** `app.js:449` сейчас жёстко использует `Intl.DateTimeFormat("ru", …)`
при английском интерфейсе. Форматирование переводится на текущую локаль.

## Глоссарий

Источник правды — `public/i18n/glossary.js`. Полные списки приведены в
приложении к этой спеке; после реализации авторитетным становится `glossary.js`,
а спека остаётся снимком принятых решений.

`docs/i18n-glossary.md` содержит политику перевода и обоснования. Списки
терминов он не дублирует, а ссылается на `glossary.js`, чтобы два источника не
разъезжались.

### Что защищено и почему

- **Термины правил и рейтинга** — то, что игрок сверяет с рулбуком: перевод
  разорвал бы связь с источником.
- **Имена собственные** — названия продуктов и фич.
- **Tac Ops, Crit Ops, Killzones** — запрет не только терминологический, но и
  технический. Эти значения одновременно видимы в интерфейсе и сохраняются как
  данные: они рендерятся как `<option>` в форме результата (`app.js:4495`,
  `app.js:4504`), в турнирной форме результата (`app.js:4738`, `app.js:4747`), в
  настройке столов (`app.js:6318`, `app.js:6349`) и в модалке настройки раунда
  (`app.js:7499`, `app.js:7511`), а `tacOpOptions` дополнительно питает
  комбобокс (`app.js:5255`) и обратный поиск названия на экране статистики
  (`app.js:3864`). `killzone` лежит в БД колонкой `TEXT`
  (`src/db/migrations/007_tournament_venue_tables.js:18`), названия Tac Op
  попадают в сохранённые результаты игр и служат ключами агрегации в
  `renderTacOpWinrates`. Перевод пункта списка изменил бы и сохраняемое
  значение — уже записанные игры разошлись бы с новыми, а агрегаты разбились.
- **Названия отрядов** — они же имена файлов в `public/kill-team-logos/`,
  поэтому перевод сломал бы отображение логотипов.

### Разбор двух неочевидных случаев

**`Classified` / `Non-Classified`** остаются английскими. Это не термин правил, а
классификация отрядов внутри All Kill Team Challenge — имя собственное фичи
проекта. Встречаются как подписи вкладок (`app.js:3883`), значения фильтра
(`app.js:3259`, `app.js:3260`) и метка классификации отряда (`app.js:3454`).

**`Faction`** переводится как «Фракция», но перевод касается **только подписи
поля**: `comboField("Faction", …)` (`app.js:938`, `app.js:6395`) и заглушки
`"Faction TBD"` (`app.js:1249`, `app.js:1272`, `app.js:6191`, `app.js:6435`) →
«Фракция не выбрана». Значение поля — это название отряда, оно защищено и
остаётся английским.

## Тесты

`test/unit/i18n.test.js`, семь проверок. Тесты не требуют базы и попадают в
`npm run test:unit`.

1. **Полнота ключей.** Множества ключей `en` и `ru` совпадают в обе стороны.
2. **Непустые значения.** Ни одно значение не пустая строка. Плюральные объекты
   содержат все формы, которых требует `Intl.PluralRules` для своей локали:
   две для английского, четыре для русского.
3. **Защищённые термины.** Если `en[key]` содержит термин из PROTECTED, то
   `ru[key]` обязан содержать его дословно и столько же раз. Сравнение с учётом
   регистра; для многословных терминов (`Kill Team`, `Approved Ops`, названия
   отрядов) границы слова проверяются по краям всей фразы, а не каждого слова.
4. **Блэклист неверных переводов.** `ru[key]` не содержит ни одной строки из
   FORBIDDEN: «килл тим», «килл-тим», «убойная команда», «очки победы», «крит
   оп», «критическая операция», «зона убийства». Ловит случаи, где термин попал
   в исходную строку в нижнем регистре или в составе словосочетания и проверка 3
   его не увидела.
5. **Плейсхолдеры.** Набор `{…}` в `ru[key]` совпадает с набором в `en[key]`.
   Потеря `{n}` — самая незаметная поломка перевода: строка выглядит целой, но
   число со страницы исчезает.
6. **HTML-безопасность.** Значения не содержат сырых `<`, а `&` встречается
   только как часть валидной HTML-сущности.
7. **Непереведённые остатки.** Совпадение `ru[key] === en[key]` допустимо
   только если значение целиком состоит из защищённых терминов, чисел и
   пунктуации. Иначе ключ обязан быть в явном списке `SAME_IN_BOTH` с
   комментарием-обоснованием. Это и есть отдельная проверка полноты перевода:
   забытая строка не проходит молча.

Тесты 3, 4 и 7 вместе покрывают требование «проверять терминологию отдельно»:
третий не даёт перевести защищённый термин, четвёртый ловит перевод в обход
дословного совпадения, седьмой не даёт пропустить строку целиком.

## Переключатель языка

Сейчас `.theme-toggle` позиционируется самостоятельно (`position: fixed`,
`top: 14px`, `right: 18px`, `styles.css:913`), и места для второй кнопки рядом с
ним нет. Оба контрола заворачиваются в общий контейнер `.floating-controls` —
flex-строку с тем же фиксированным позиционированием.

- Кнопка 44×44, визуально идентична кнопке темы, с текстовой меткой `RU`/`EN`.
- Метка показывает **текущий** язык, подсказка — действие. Изначально было
  наоборот (метка = целевой язык, как у кнопки темы, которая показывает иконку
  целевой темы), но это оказалось ошибкой: двухбуквенный код языка читается как
  индикатор состояния, а не как действие, и на английском сайте надпись «RU»
  выглядела утверждением, что сайт русский. Иконка темы так не читается,
  поэтому у неё соглашение осталось прежним.
- `title` и `aria-label` локализованы («Switch to Russian» / «Переключить на
  английский»).
- По клику: запись в `localStorage` под ключом `tgtv-locale` (зеркалит
  существующий `tgtv-theme`), обновление `document.documentElement.lang`,
  `render()`.
- Логика повторяет `savedThemePreference` / `applyTheme` / `wireThemeToggle`
  (`app.js:7857`), включая `try/catch` вокруг `localStorage` для браузеров с
  заблокированным хранилищем: при недоступном хранилище язык применяется на
  текущую страницу и не сохраняется.

На экранах ≤560px обе кнопки ужимаются до 38×38, а `.topbar { padding-right }`
растёт с 64px до 92px (`styles.css:2973`). Это единственное место в задаче,
которое нужно проверить визуально, а не тестом.

Переключатель доступен на всех экранах, включая форму входа и публичную
страницу турнира, потому что контейнер живёт в `index.html`, а не внутри
`renderShell`.

## Порядок работ

Миграция идёт по одной render-функции за раз. Каждый шаг оставляет приложение
рабочим: отсутствующий русский ключ даёт английский текст, а не поломку.

1. **Вынос справочников.** `public/game-data.js` и подключение в `index.html`.
   Поведение не меняется.
2. **Каркас.** Рантайм, пустые словари, глоссарий, тесты, переключатель. На
   этом шаге `ru.js` пуст, всё уходит в английский фолбэк, поведение
   приложения не меняется.
3. **Таблицы-константы.** `standingsTiebreakerOptions` с полями `label` и
   `description` (`app.js:55`) и `opLabels` (`app.js:89`) вычисляются один раз
   при загрузке файла, поэтому готовые строки в них не переживут смену языка:
   переключение их не перерисует до перезагрузки страницы. Они переводятся из
   «объектов с текстом» в «объекты с ключами», а `t()` вызывается в момент
   рендера. Делается до экранов, иначе ошибка размножится по всем вызовам.
   `venueModeOptions` приведена к тому же виду ещё на шаге 1.
4. **Общие хелперы и навигация** — `common.*`, `nav.*`.
5. **Каркас и вход** — `renderShell`, `renderAuth`.
6. **Экраны по одному** — `play`; `games` вместе с `renderResultForm`,
   `renderResultReview`, `renderGameDetail`; `tournaments` вместе с публичной
   страницей, формой участия и турнирными формами результата; `statistics`;
   `challenge`; `profile` и `playerProfile`; `feedback`; `admin` вместе с
   `renderRoundSetupModal`.
7. **Диалоги и сообщения** — 16 вызовов `confirm()` и 10 клиентских
   `setMessage("…")` с литералами.
8. **Финал** — прогон теста на непереведённые остатки, вычитка формулировок на
   единообразие тона, обновление версионных суффиксов `?v=` в `index.html`.

## Риски

- **Пропущенная строка.** Основной риск задачи. Снимается тестом 7: ключ,
  совпадающий в обеих локалях, обязан быть явно оправдан.
- **Строка, не проходящая через `t()`.** Тест 7 её не увидит, потому что она не
  попала в словарь. Снимается пошаговой миграцией по одной render-функции и
  финальным grep-проходом по литералам в `app.js` на шаге 8.
- **Разъезжание длин.** Русский текст в среднем длиннее английского на 10–15%,
  что может ломать вёрстку узких элементов — вкладок, кнопок в строках таблиц,
  заголовков колонок. Проверяется визуально после шага 6 на каждом экране;
  правки — в `styles.css`, без изменения словарей.
- **Смешанный язык при ошибках валидации.** Следствие выбранного объёма, описано
  в разделе «Объём».

## Приложение: полный список терминов

### PROTECTED — остаются английскими (103 значения)

**Термины правил и рейтинга (18).** Перечисляются в `glossary.js` явно. Формы
множественного числа заведены отдельными записями: проверка идёт по границам
слова, поэтому `Tac Ops` и `Tac Op` — разные термины, и без обеих записей
множественное число осталось бы без защиты.

`Kill Team` · `Kill Teams` · `Approved Ops` · `Crit Op` · `Crit Ops` ·
`Kill Op` · `Kill Ops` · `Tac Op` · `Tac Ops` · `Primary Op` · `Primary Ops` ·
`VP` · `Killzone` · `Killzones` · `Dataslate` · `Elo` ·
`Strength of Schedule` · `SoS`

**Имена собственные (7).** Явно:

`Warhammer 40k Kill Team` · `Tabletop Simulator` · `All Kill Team Challenge` ·
`Classified` · `Non-Classified` · `Wildcard` · `Wildcards`

`Wildcard` добавлен по итогам реализации: это название механики челленджа, и
держать его английским через `SAME_IN_BOTH` было недостаточно — тот список
оправдывает конкретные ключи, а не термин. В `PROPER_NAMES` его защищает
проверка 3 в любом будущем ключе.

**Tac Ops (12).** Из `game-data.js`:

Plant Devices · Steal Intelligence · Track Enemy · Flank · Retrieval ·
Scout Enemy Movement · Plant Banner · Martyrs · Envoy · Rout · Sweep & Clear ·
Dominate

**Crit Ops (9).** Из `game-data.js`:

Secure · Loot · Transmission · Orb · Stake Claim · Energy Cells · Download ·
Data · Reboot

**Killzones (8).** Из `game-data.js`:

Volkus · Gallowdark · Bheta-Decima · Octarius · Tomb World · WTC ITD ·
WTC Open · Non-specific

**Сезоны (1).** Из `game-data.js`: `2026 Q2 Dataslate`

**Названия отрядов (48).** Из `KILL_TEAMS` в `src/domain/kill-teams.js`:

Novitiates · Elucidian Starstriders · Hunter Clade · Death Korps ·
Phobos Strike Team · Gellerpox Infected · Legionaries · Blooded · Warpcoven ·
Corsair Voidscarred · Wyrmblade · Void-Dancer Troupe · Kommandos · Pathfinders ·
Kasrkin · Inquisitorial Agents · Exaction Squad · Angels of Death · Chaos Cult ·
Fellgor Ravagers · Hand of the Archon · Farstalker Kinband ·
Hearthkyn Salvagers · Hierotek Circle · Scout Squad · Blades of Khaine ·
Plague Marines · Mandrakes · Nemesis Claw · Brood Brothers · Hernkyn Yaegirs ·
Tempestus Aquilons · Wrecka Krew · Vespid Stingwings · Ratlings · Sanctifiers ·
Goremongers · Raveners · Battleclade · Deathwatch · Canoptek Circle ·
Wolf Scouts · Celestian Insidiants · Murderwing · Spectre Squad ·
Dragon Masters · Navy Breachers · XV26 Stealth Battlesuits

### TRANSLATED — турнирный и общий слой

| Английский | Русский | Комментарий |
| --- | --- | --- |
| Round | Раунд | |
| Standings | Таблица | |
| Leaderboard | Таблица лидеров | |
| Swiss | Швейцарка | |
| Single elimination | На выбывание | |
| Buchholz | Бухгольц | шахматный термин, устоявшийся в русском |
| Head-to-head | Личная встреча | |
| Bye | Бай | |
| Seed | Посев | |
| Tournament Points | Турнирные очки | |
| Total VP | Всего VP | `VP` защищён внутри строки |
| VP Diff | Разница VP | то же |
| Winrate | Винрейт | |
| Rating | Рейтинг | |
| Matchmaking | Подбор соперника | |
| Challenge | Вызов | в смысле «вызвать игрока»; в составе `All Kill Team Challenge` не переводится |
| Game | Игра | |
| Match | Матч | турнирная игра |
| Participant | Участник | |
| Faction | Фракция | только подпись поля; значение — название отряда, защищено |
| Table | Стол | физический стол в турнире |
| Season | Сезон | |
| In Real Life | Вживую | |
| Feedback | Обратная связь | |
| Profile | Профиль | |
| Stats | Статистика | |
| Sign out | Выйти | |

### FORBIDDEN — блэклист для теста 4

«килл тим» · «килл-тим» · «убойная команда» · «очки победы» · «крит оп» ·
«критическая операция» · «зона убийства»

Список пополняется по мере находок при вычитке.
